import React from 'react';

const Projects = () => {
    return (
        <section id="about" className="h-auto min-h-[300px] bg-neutral flex flex-col npm justify-end items-center p-8">
            Projects
        </section>

    );
};

export default Projects;
